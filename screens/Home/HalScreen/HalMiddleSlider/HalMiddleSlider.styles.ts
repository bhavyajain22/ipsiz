import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  carouselSingleItem: {
    height: 150,
    width: 150,
    resizeMode: 'cover',
    borderRadius: 5,
  },
  carouselContainer: {
    overflow: 'hidden',
    marginLeft: 20,
  },
});
